// Triple Equals ( === )  returns true if both operands are of the same type and contain the same value
// Double Equals ( == ) checks for value equality only.
const first = 2;
const second = '2';
if (first === second) {
    console.log('Condition is True');
    
}

else{
    console.log('Condition is false');
}